name: example_dart_pub
version: 1.0.0
description: >-
  Dart project with a mix of safe, vulnerable, runtime, and dev
  dependencies for bomly QA.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  path: ^1.9.0
  # archive 3.3.0: CVE-2023-36090 (directory traversal via crafted ZIP)
  archive: 3.3.0
  # http 0.12.0: old release with known security gaps
  http: 0.12.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.8
